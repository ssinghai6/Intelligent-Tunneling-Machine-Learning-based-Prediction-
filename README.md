# Intelligent-Tunneling-Machine-Learning-based-Prediction-
Evaluating the stress, elastic and plastic strain fields around the cavity at the new location
Based on Inputs: position, geometry of robot (modeled as cavity), soil   parameters or sensor data (e.g., pressure-volume curve)

Bypass the simulation and generate the output (Stresses) from the trained ML model.
